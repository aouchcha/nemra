# Nemra Frontend

Next.js app router frontend for the Nemra backend.

## Run

```bash
cd frontend
npm install
npm run dev
```

## Environment

Set `NEXT_PUBLIC_API_BASE_URL` to the Spring Boot API origin.

Default:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
```
